module.exports = {
    prepareTrainingData: async (contracts, markdownContent) => {
      // For now, just simulate the preparation of training data
      return { contracts, markdownContent };
    },
  };
  